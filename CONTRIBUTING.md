# 🤝 Руководство для контрибьюторов

Спасибо за интерес к проекту **BIOPRINTER**!

---

## 📋 Как внести вклад

### 1. Форкните репозиторий

```bash
# Клонируйте ваш форк
git clone https://github.com/YOUR_USERNAME/bioprinter-site.git
cd bioprinter-site
```

### 2. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-name
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Внесите изменения

- Следуйте стилю кода проекта
- Пишите понятные commit-сообщения
- Тестируйте изменения локально

### 5. Проверьте код

```bash
# Линтинг
npm run lint

# Проверка типов
npm run typecheck

# Сборка
npm run build
```

### 6. Создайте Pull Request

- Опишите, что было изменено
- Укажите причину изменений
- Приложите скриншоты (если UI-изменения)

---

## 🎨 Стандарты кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`
- Экспортируйте типы/интерфейсы

```tsx
// ✅ Хорошо
interface CardProps {
  title: string
  children: ReactNode
}

// ❌ Плохо
function Card(props: any) { }
```

### React

- Используйте функциональные компоненты
- Hooks вместо классов
- Деструктуризация props

```tsx
// ✅ Хорошо
export default function Card({ title, children }: CardProps) {
  return <div>{title}</div>
}

// ❌ Плохо
export default function Card(props) {
  return <div>{props.title}</div>
}
```

### CSS/Tailwind

- Используйте Tailwind-классы
- Избегайте inline-стилей
- Используйте `clsx` для условных классов

```tsx
// ✅ Хорошо
<div className={clsx('card', isActive && 'bg-primary-50')}>

// ❌ Плохо
<div style={{ backgroundColor: isActive ? '#EBF2FE' : '' }}>
```

---

## 📝 Commit Guidelines

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить новую страницу
fix: исправить ошибку в роутинге
docs: обновить README
style: форматирование кода
refactor: рефакторинг компонента Card
perf: оптимизация анимаций
test: добавить тесты
chore: обновить зависимости
```

---

## 🧪 Тестирование

### Ручное тестирование

1. Проверьте все языки (ru/uz/en)
2. Проверьте адаптивность (mobile/tablet/desktop)
3. Проверьте анимации
4. Проверьте все ссылки

### Lighthouse

```bash
npm run build
npm run preview
lighthouse http://localhost:4173 --view
```

Целевые метрики:
- Performance: ≥ 95
- Accessibility: ≥ 90
- Best Practices: ≥ 95
- SEO: ≥ 95

---

## 📁 Структура файлов

```
src/
├── components/
│   ├── ui/              # Базовые UI-компоненты
│   ├── layout/          # Layout-компоненты
│   └── [Feature].tsx    # Специфичные компоненты
├── pages/               # Страницы
├── content/             # DATA_SOURCE
├── i18n/                # Переводы
└── App.tsx              # Главный компонент
```

---

## ⚠️ Важные ограничения

1. **НЕ изменяйте DATA_SOURCE без согласования**
   - Файл `src/content/data.ts` — единственный источник данных
   - Все изменения должны соответствовать образовательным требованиям

2. **НЕ добавляйте сторонний контент**
   - Только данные из утверждённого DATA_SOURCE
   - Никакого текста с других сайтов (Cellink, Brinter и т.д.)

3. **НЕ используйте внешние API**
   - Проект должен работать offline (SSG)

---

## 🐛 Сообщение о багах

При обнаружении бага создайте Issue с:

- **Описанием:** что случилось
- **Шаги воспроизведения:** как повторить
- **Ожидаемое поведение:** что должно было произойти
- **Скриншоты:** если применимо
- **Окружение:** браузер, ОС, версия

---

## 💡 Предложение фич

Создайте Issue с тегом `feature request`:

- **Описание:** что добавить
- **Обоснование:** зачем это нужно
- **Альтернативы:** другие подходы

---

## 📞 Контакты

- Email: info@bioprinter.uz
- Issues: [GitHub Issues](https://github.com/yourusername/bioprinter-site/issues)

---

**Спасибо за вклад! 🙏**

