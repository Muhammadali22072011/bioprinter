# 🚀 Деплой на Vercel

## Подготовка проекта завершена! ✅

Проект настроен и готов к деплою на Vercel с использованием Vite.

## 📋 Что было сделано

1. ✅ Создан `vercel.json` с конфигурацией для:
   - SPA роутинга (React Router)
   - Оптимизации кэширования статических ресурсов
   - Правильной обработки всех маршрутов

2. ✅ Создан `.vercelignore` для исключения ненужных файлов

## 🌐 Способы деплоя

### Вариант 1: Через Web UI (Рекомендуется)

1. **Перейдите на** [vercel.com](https://vercel.com)
2. **Войдите** через GitHub/GitLab/Bitbucket
3. **Нажмите** "Add New Project"
4. **Импортируйте** ваш Git репозиторий
5. **Vercel автоматически определит** настройки:
   - Framework Preset: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. **Нажмите** "Deploy"

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm install -g vercel

# Запустите деплой
vercel

# Для продакшн деплоя
vercel --prod
```

## ⚙️ Настройки проекта

### Build Settings (автоматически определяются)
- **Framework:** Vite
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

### Environment Variables (если нужны)
Добавьте в настройках проекта на Vercel:
- Перейдите в Settings → Environment Variables
- Добавьте необходимые переменные окружения

## 🔍 Проверка перед деплоем

Убедитесь, что проект собирается локально:

```bash
# Установите зависимости
npm install

# Соберите проект
npm run build

# Проверьте сборку локально
npm run preview
```

## 📦 Что включает деплой

- ✅ React приложение с TypeScript
- ✅ React Router для навигации
- ✅ i18n для мультиязычности (EN, RU, UZ)
- ✅ Tailwind CSS для стилей
- ✅ Framer Motion для анимаций
- ✅ SEO оптимизация
- ✅ PWA поддержка

## 🌍 После деплоя

После успешного деплоя вы получите:
- 🔗 Production URL: `https://your-project.vercel.app`
- 🔄 Автоматический деплой при каждом push в main/master
- 📊 Analytics и мониторинг
- 🚀 CDN для быстрой загрузки по всему миру

## 🎯 Полезные команды

```bash
# Проверка типов
npm run typecheck

# Линтинг кода
npm run lint

# Локальная разработка
npm run dev

# Preview продакшн сборки
npm run preview
```

## 📝 Дополнительно

### Настройка Custom Domain
1. Перейдите в Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям

### Автоматический деплой
Vercel автоматически создаст деплой для:
- ✅ Каждого коммита в main/master (production)
- ✅ Каждого Pull Request (preview)

## ❓ Возникли проблемы?

- Проверьте логи сборки на Vercel
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что проект собирается локально командой `npm run build`

---

**Готово к деплою!** 🎉


