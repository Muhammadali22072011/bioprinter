# 🚀 Команды для cmd.exe (Command Prompt)

## ⚠️ ПРОБЛЕМА: Ошибка "name consists only of disallowed characters"

Эта ошибка возникает, когда git не настроен правильно (нет user.name или user.email).

---

## ✅ ШАГ 1: Настройте git (ОБЯЗАТЕЛЬНО!)

```cmd
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

**Ваши данные:**
```cmd
git config --global user.name "Muhammadali22072011"
git config --global user.email "nasiban914@gmail.com"
```

---

## ✅ ШАГ 2: Проверьте настройки

```cmd
git config user.name
git config user.email
```

Должны показать ваши имя и email.

---

## ✅ ШАГ 3: Создайте коммит

Теперь попробуйте создать коммит:

```cmd
git commit -m "Initial commit"
```

Если это всё ещё не работает, используйте без кавычек:

```cmd
git commit -m Initial
```

Или создайте коммит без сообщения (откроется редактор):

```cmd
git commit
```

В редакторе (обычно vim или nano):
- Если это vim: нажмите `i`, напишите сообщение, нажмите `Esc`, затем введите `:wq` и Enter
- Если это nano: напишите сообщение, нажмите `Ctrl+O` (сохранить), `Ctrl+X` (выйти)

---

## 📋 Полная последовательность команд для cmd.exe:

```cmd
cd "C:\Users\User\Desktop\BioPrinter SIte"

REM Настройте git (если ещё не настроено)
git config --global user.name "Muhammadali22072011"
git config --global user.email "nasiban914@gmail.com"

REM Настройте remote
git remote remove origin
git remote add origin https://github.com/Muhammadali22072011/bioprinter.git

REM Добавьте файлы
git add .

REM Создайте коммит
git commit -m "Initial commit"

REM Если коммит не создался, попробуйте:
REM git commit -m Initial
REM или
REM git commit

REM Настройте ветку
git branch -M main

REM Загрузите на GitHub
git push -u origin main
```

---

## 🔧 Если проблема с кавычками продолжается:

### Вариант 1: Используйте короткое сообщение
```cmd
git commit -m "Initial"
```

### Вариант 2: Создайте коммит без сообщения (откроется редактор)
```cmd
git commit
```
Затем в открывшемся редакторе напишите сообщение и сохраните (Ctrl+S, затем закройте).

### Вариант 3: Используйте переменную окружения
```cmd
set MSG=Initial commit: BioPrinter educational site
git commit -m "%MSG%"
```

---

## ✅ Проверка после коммита:

```cmd
git log --oneline
```

Должен показать ваш коммит. Если видите коммит - значит всё готово для push!

---

**Попробуйте сначала самое простое:**
```cmd
git commit -m "Initial"
```

---

## ✅ ШАГ 4: Загрузите на GitHub

### ⚠️ Если удаленный репозиторий НЕ пустой (ошибка "rejected"):

**Вариант 1: Объединить изменения (рекомендуется)**
```cmd
git pull origin main --allow-unrelated-histories
```
Если возникнут конфликты, разрешите их, затем:
```cmd
git push -u origin main
```

**Вариант 2: Перезаписать удаленный репозиторий (ОСТОРОЖНО!)**
```cmd
git push -u origin main --force
```
⚠️ Это удалит всё, что есть на GitHub, и заменит вашим локальным проектом!

### Если удаленный репозиторий пустой:

```cmd
git push -u origin main
```

---

## 🎉 После успешного push:

Проект будет доступен по адресу:
**https://github.com/Muhammadali22072011/bioprinter**

