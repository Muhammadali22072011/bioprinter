# ⚡ Быстрый старт

## За 3 минуты до первого запуска

### 1️⃣ Установите Node.js (если ещё не установлен)

**Windows:**
- Скачайте с https://nodejs.org/ (LTS версия)
- Установите с настройками по умолчанию

**macOS:**
```bash
brew install node
```

**Linux:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 2️⃣ Откройте проект в терминале

**Windows (PowerShell):**
```powershell
cd "C:\Users\User\Desktop\BioPrinter SIte"
```

**macOS/Linux:**
```bash
cd ~/Desktop/BioPrinter\ SIte
```

### 3️⃣ Установите зависимости

```bash
npm install
```

⏱️ Это займёт 2-3 минуты при первом запуске.

### 4️⃣ Запустите dev-сервер

```bash
npm run dev
```

✅ Откройте в браузере: **http://localhost:5173**

---

## 🎯 Основные команды

```bash
# Разработка (с hot-reload)
npm run dev

# Production-сборка
npm run build

# Предпросмотр production-сборки
npm run preview

# Проверка кода (линтинг)
npm run lint

# Проверка типов TypeScript
npm run typecheck
```

---

## 📝 Быстрое редактирование

### Изменить контент (рецепты, характеристики, etc.)

Откройте файл:
```
src/content/data.ts
```

### Изменить UI-строки (кнопки, меню)

Откройте файлы:
```
src/i18n/locales/ru.json   (русский)
src/i18n/locales/uz.json   (узбекский)
src/i18n/locales/en.json   (английский)
```

### Изменить цвета/стили

Откройте файл:
```
tailwind.config.js
```

---

## 🚀 Production Build

```bash
# 1. Создать production-сборку
npm run build

# 2. Результат будет в папке dist/
# Готово к деплою на любой статический хостинг!
```

---

## 🆘 Частые проблемы

### Ошибка "npm не найдена"

Установите Node.js (см. шаг 1 выше).

### Ошибка "port 5173 already in use"

Закройте другие экземпляры dev-сервера или измените порт:
```bash
npm run dev -- --port 3000
```

### Ошибка "Cannot find module"

Переустановите зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 Подробная документация

- **README.md** — полная документация проекта
- **DEPLOYMENT.md** — руководство по деплою
- **CONTRIBUTING.md** — для контрибьюторов

---

**Готово! Сайт запущен. 🎉**

Откройте http://localhost:5173 в браузере.

